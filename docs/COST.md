# コスト計測（2026-09-21）

同じ 20 メッセージ（日程 5・本人に聞く 10・引っ込む 3・雑談 2、うち 1 件はプロンプトインジェクション）を、キャラ「ヒデじい」で 3 構成に流した。
生データ: `data/replay-2026-09-21-10-12.md`。LLM 呼び出しは全件 `llm_log`（要求モデル・実際のモデル・トークン・レイテンシ・OrcaRouter ヘッダ）。

| 構成 | 実際に使われたモデル | 呼び出し | prompt tok | completion tok | 平均レイテンシ | 判断一致 | 20件のコスト | **1件あたり** |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| `deepseek/deepseek-v4.1-flash`（採用） | deepseek-v4.1-flash | 25 | 77,799 | 9,391 | **3.1 秒** | 16/20 | $0.017 | **約 0.13 円** |
| `orcarouter/auto` | glm-5.3-flash ×24, claude-opus-5 ×1, deepseek-flash ×1 | 26 | 79,702 | 12,746 | 15.6 秒 | 15/20 | 約 $0.04 | 約 0.28 円 |
| `anthropic/claude-sonnet-5` | claude-sonnet-5 | 25 | 127,173 | 5,891 | 7.3 秒 | 16/20 | $0.313 | **約 2.35 円** |

単価は OrcaRouter `/v1/models` の pricing（USD/token）、1 USD = 150 円で換算。auto は glm-5.3-flash 単価で概算し opus 1 回分を上乗せ。

## 読み方

- **判断の質は 3 構成でほぼ同じ**（16/20 前後）。外した 4 件は「契約更新の相談 → 引っ込む」「ランチの誘い → 日程」など、期待値側が割れる境界ケース。
- **速さは deepseek-flash が 2〜5 倍速い**。auto は glm-5.3-flash（推論モデル）に振られ、1 件 15 秒・最大 64 秒。Slack の返信としては待てない。
- **コストは deepseek-flash が sonnet の 1/18**。判断が同じなら、1 件 0.13 円で回る。
- sonnet は prompt tok が 1.6 倍（同じ入力で、ツール定義のトークン数の数え方が違う）。

## 採用した構成

| 用途 | モデル | 理由 |
|---|---|---|
| 判断・返信文・伝聞・主人との DM | `deepseek/deepseek-v4.1-flash` | 速い・安い・判断は上位モデルと同等 |
| 主人名義の文案（謝罪など） | `anthropic/claude-opus-4.8` | 頻度が低く品質が要る |
| フォールバック | `orcarouter/auto` → `anthropic/claude-sonnet-5` | 主モデルが落ちたとき（9/20 に実際に発生） |

## LLM を呼ばない経路（コストゼロ）

- 「ありがとう」「了解です」などの締めの言葉 → 定型文
- 選択肢のボタン（候補を出し直す・短縮・この候補で入れる） → コードで実行
- 相手のじいやからの招待 → 定型文
- じいや同士の日程が決まった後の締め → 定型文

システムプロンプトは約 1,800 トークン。1 件あたり平均 1.25 回の LLM 呼び出し（日程は 2 回、他は 1 回）。

## OrcaRouter ダッシュボードとの突合

開発 3 日間の全呼び出し（プロンプト調整・Slack デモ・上の計測）の合計: **$0.39 / $20**（2026-09-22 06:45 時点）。
計測 3 構成の単価計算 ≈ $0.37 と整合。
